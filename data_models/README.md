# Data Models

This directory contains all data model versions generated during development. Data models should always be stored in JSON format, but may also be stored in additional formats such as Mermaid.